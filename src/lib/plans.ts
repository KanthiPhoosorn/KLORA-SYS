// Subscription default for NEW farm registrations.
// Pilot (from 21 Sep 2026): every farm starts on "pro" so the trial farms see the full product —
// overview, carbon dashboard, full history and every carrier. KYN can still downgrade a farm to
// "free" from จัดการผู้ใช้งาน → แพ็กเกจ. Flip this to "free" when billing goes live.
export const DEFAULT_PLAN: "free" | "pro" = "pro";

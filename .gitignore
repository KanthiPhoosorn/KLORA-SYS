# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env*.local

# typescript
*.tsbuildinfo

# runtime JSON store (seed files are committed; live writes are ignored)
/data/*.local.json
/data/*.tmp
/data/otp.json

# installed agent skills (third-party — keep out of the portfolio repo)
.agents/

# stray/local secret + tooling (never commit)
*.env.local
KLORA-SYS.env.local
.claude/
skills-lock.json
